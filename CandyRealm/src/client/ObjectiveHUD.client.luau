--======================================================================
-- ObjectiveHUD.client.luau   (StarterPlayerScripts)  -- CandyRealm
--======================================================================
-- REMOVED at the owner's request: this built the top-centre onboarding card -- "🛒 BUY BEANS AT THE
-- FOOD STAND", the fart/ring coaching steps, the full-tank coin readout -- and now builds nothing.
--
-- It was also the wrong card for this realm: the copy was carried over from realm 1 wholesale, coaching
-- players to buy BEANS and graduate at "Broccoli Bluff" in a realm whose first island is Candy Cane
-- Court and whose shop sells candy. Removing it here loses nothing that was true.
--
-- The file is kept as a stub rather than deleted so the realm's script manifest stays stable and this
-- note survives; realm 1's copy (src/client/ObjectiveHUD.client.luau in the main game) is untouched and
-- remains the reference if a Candy-worded onboarding card is ever wanted -- port THAT and re-word it,
-- rather than resurrecting this one.
--
-- Layout note for whoever reclaims the space: NotifyCenter's top-centre budget reserved y 85..163 for
-- this card (see its header). With the card gone that band is free -- but anything new there must still
-- go through a NotifyCenter lane, or it will collide with the hero banner exactly the way the old quest
-- banners did.
--======================================================================

print("[ObjectiveHUD] removed -- no onboarding card in the Candy realm (see the stub header)")
